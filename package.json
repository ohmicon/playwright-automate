{
  "name": "playwright-automate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf test-results && rimraf playwright-report",
    "test:single": "npm run clean && playwright test --headed --workers 1",
    "test:parallel": "npm run clean && playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.4.1",
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.14.2",
    "@types/xlsx": "^0.0.36",
    "rimraf": "^5.0.7"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "xlsx": "^0.18.5"
  }
}
